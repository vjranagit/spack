config:
  db_lock_timeout: 120
  install_tree:
    root: /home/software/spack
    padded_length: 256
    projections:
      all: "{architecture.platform}/{architecture.target}/{name}-{version}-{hash}"

